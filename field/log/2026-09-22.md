# Field log — 2026-09-22

**Phase:** 0, set up (entering 1, understand)

1. Installed niha v1.3.7 via Homebrew, signed in, org resolved as AI Challenge Q3 2026,
   role capability_builder. `niha doctor` green apart from the expected workspace notice.
2. Where the tool helped: nothing model-backed today, because none of it ran. The
   non-model surfaces (`doctor`, `whoami`, `status`, `--version`) were fast, clear and
   accurate, and `doctor`'s per-check output is genuinely good diagnostics.
3. Where it cost time, and how much: ~35 min on F-01 — `doctor` told me to run
   `niha init`, which the setup guide explicitly forbids for this situation, and which
   cannot succeed. That led into ~20 min on F-02, a dead-end admin-only page the web
   console pointed me at. Both were avoidable had `doctor` not given directive advice
   that contradicts the documented path.
4. Then the blocker: F-03. Every model call fails with a provider usage-limit 400,
   reset date 2026-10-01 — six days after submissions close. Reproduced 8 times across
   `niha ask` and the interactive session, on my own shell and independently in a plain
   terminal. 1s per attempt, $0.00 charged, so it is a hard pre-flight cut, not a
   throttle I can wait out inside a session.
5. Escalated F-03 to the challenge channel the same day, per the T1 rule, rather than
   burning the ninety minutes retrying.
6. Also recorded F-04: the interactive session reports `0 tools` where the setup guide's
   example shows `14 tools`. Cannot tell yet whether that is cosmetic or means no tools
   are registered at all, because no turn ever reaches tool selection while F-03 stands.
7. Tool switch declared (see `field/tool-switches.md`): all model-backed work moved to
   Claude Code for the duration of F-03. niha remains first choice and is retried.
8. Observation IDs logged today: F-01 (#1012), F-02 (#1011 fix PR, issue #1010),
   F-03 (#1013), F-04 (#1014).
9. Product side: repository `amrutha-kvb/parkmitra` created, public, remote configured,
   artifact folder structure created per the process spec. No product code yet — scope
   is deliberately still open until Gate 2.
10. Honest note on the shape of this challenge run: registration was late and the build
    window remaining is roughly three days, not ten. Scope will be cut accordingly at
    Gate 2 and the reduction stated plainly rather than hidden.
